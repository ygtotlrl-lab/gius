# גיוס

אפליקציית PWA לניהול גיוס כספים. עברית מלאה, RTL, מותאמת למובייל ולדסקטופ.

**כתובת האפליקציה** — `android.url` שבתצורה.

## הפעלה ראשונה

1. הריצו את `migrations/000_schema.sql` מול פרויקט ה-Supabase
   (`supabase.url` שבתצורה) דרך ה-SQL Editor — הוא הסכימה החיה, והרצה חוזרת
   אינה משנה דבר.
2. צרו את המשתמש הראשון **ידנית** ב-SQL Editor — ⛔ אין משתמש ברירת מחדל ואין
   סיסמה בריפו. הטביעה נגזרת בקונסולה של הדף עצמו:
   `(await import('./core/auth.js')).authMakePassFp('<שש ספרות>')` מחזירה
   `{salt, fp}`, והם נכנסים ל-`pass_salt` ול-`pass_fp` של שורה ב-`g_users`,
   עם `role` שהוא `'admin'`.
3. במסך ההגדרות: קבעו יעד חודשי, ערכו את רשימות הבחירה, והוסיפו משתמשים.

## מסכים

- **בית** — מצב החודש מול היעד, פילוח לפי סעיף ולפי שגריר, ו"מה שלי היום".
- **תורמים** — חיפוש, כרטיס תורם עם התחייבויות ותנועות.
- **התחייבויות** — רשימה עם מסננים וסטטוס מחושב.
- **משימות** — קנבן בארבעה שלבים עם יומן מצטבר.
- **הגדרות** — משתמשים, רשימות ויעדים (בעלים בלבד).

## פיתוח

הכל בקובץ אחד: `index.html`. אין build.

```bash
node tools/guard.mjs       # שומר הדחיפה — חובה לפני כל push
node tools/gen-app.mjs     # מחדש מהתצורה את קובצי הפלטפורמה והאייקונים
```

<!-- SHARED:start id="readme-gate" -->
השומר בודק את תחביר הסקריפט הראשי, `sw.js` ו-`core/`, ⛔ ושכל קובץ ברשימת
המטמון קיים — ⚠️ ורץ גם בכל דחיפה ובכל PR.
<!-- SHARED:end -->

<!-- SHARED:start id="readme-apk" -->
## APK

מעטפת אנדרואיד מסוג **WebView** (⛔ לא TWA) ב-[`android/`](android/README.md),
שטוענת את כתובת ה-Pages מהרשת. בנייה: Actions → **Build APK** → Run workflow.
שחרור קוד web אינו מצריך APK חדש.

<!-- SHARED:end -->
