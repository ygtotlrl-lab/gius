-- ============================================================================
-- 010_drop_g_users_password.sql — צעד ב: מחיקת הסיסמה הגלויה
-- ============================================================================
--
-- ⛔ **נכתב ולא רץ.** ⚠️ ההרצה היא פעולת מנהל, ⛔ ואין להריץ אותה מתוך סשן.
--
-- ⭐ **מה שקדם לזה.** צעד א — הסרת ה-`NOT NULL` — רץ במסד בסבב 40, והשאיר
--    את `g_users.password` **כתובה ואינה נקראת**. ⚠️ ה-RLS כאן הוא
--    `using (true) with check (true)` בכוונה מתועדת, עם GRANT ל-`anon`
--    ומפתח `anon` שיושב ב-`index.html` הציבורי: ⛔ העמודה הייתה קריאה
--    לכל מי שפתח את קוד המקור.
--
-- ⭐ **מה שהשתנה בקוד, ⛔ ובלעדיו אין להריץ כאן דבר:** הדגל
--    `G_PLAINTEXT_LEGACY_WRITE` **ירד**, ואיתו השורה היחידה שכתבה את
--    העמודה — ב-`passFields`, שמשרתת את שלושת מסלולי הסיסמה: יצירת
--    משתמש, קביעת סיסמה ע"י בעלים, ושינוי הסיסמה העצמי. ⛔ כל השלושה
--    כותבים מעכשיו `pass_salt`+`pass_fp` בלבד.
--
-- ⚠️ **נמדד לפני הכתיבה (`SELECT` בלבד):** משתמש אחד ב-`g_users`, ולו
--    `pass_salt` ו-`pass_fp` באורך 64 — ⛔ אף אחד אינו ננעל בחוץ.
--
-- ═══ ⛔ סדר ההרצה ═══════════════════════════════════════════════════════════
--   1. הקוד שבסבב הזה נדחף ונפרס למכשירים (`CACHE_NAME` קודם).
--   2. אימות ידני: יצירת משתמש · שינוי סיסמה · כניסה אופליין.
--   3. שאילתת האימות שלמטה מחזירה `total = with_fp`.
--   4. ורק אז — המשפט היחיד שכאן.
-- ⛔ **הרצה לפני שלב 1 מפילה כל יצירת משתמש במכשיר שעדיין על הקוד הישן** —
--    הוא שולח `password` לעמודה שכבר אינה קיימת.
--
-- ⛔ **ו-`strip: ['password']` נשאר בקוד גם אחרי ההרצה** — ⚠️ מכשיר מותקן
--    מחזיק מראה שנכתבה לפני הסבב הזה, ⭐ והסינון הוא מה שמנקה אותה.
--
-- ⚠️ **והמחיקה אינה מסירה את מה שכבר נראה:** ⛔ `g_users` נגבית
--    ל-`kv_backup` ברשימת עמודות מפורשת — ⚠️ **נמדד: `password` אינו בה**,
--    ולכן אין עותק שלה בגיבוי. ⭐ והטיפול המלא הוא **החלפת הסיסמה** של
--    המשתמש, פעולת מנהל, אחרי ההרצה כאן.
-- ═══════════════════════════════════════════════════════════════════════════

-- ── אימות לפני ההרצה (SELECT בלבד) ─────────────────────────────────────────
--   select count(*)                                             as total,
--          count(*) filter (where pass_salt is not null
--                             and length(pass_fp) = 64)         as with_fp
--   from public.g_users;
--   -- ⛔ total חייב להיות שווה ל-with_fp. אם לא — ⛔ אין להריץ.

-- ── צעד ב — מחיקת העמודה ───────────────────────────────────────────────────
alter table public.g_users drop column if exists password;
